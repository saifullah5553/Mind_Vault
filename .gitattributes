# Normalize line endings: text files stored as LF in the repo, checked out
# native. Keeps diffs clean across Windows/Linux (CI runs on Linux).
* text=auto eol=lf

# Binary assets — never normalize.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.mp4 binary
*.wav binary
*.onnx binary
*.zip binary
*.db binary
