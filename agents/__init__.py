"""Mind_Vault agents. Each agent lives in its own subpackage with an `agent.py`
module and a `config.yaml`. Importing this package does not import the agents;
call `core.registry.load_all_agents()` to register them all.
"""
